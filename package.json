{
  "name": "pocketbase-migrate",
  "version": "0.2.0",
  "description": "A script for migrating records between two PocketBase instances",
  "main": "pb-migrate.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pocketbase": "^0.26.8"
  }
}
