## Working Rules

- Update the `changelog.md` file after any changes. Always include the version number. Follow semantic versioning.
- Add new general coding rules to the `agents.md` file if they exist.
- Create a `readme.md` with a general description of the project and usage examples. Update after behavior changes
- For docs use english
- After changes, always provide a commit title suggestion.
